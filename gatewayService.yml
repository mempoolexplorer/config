#Cuidado que los nombres de los servicios deben estar en MINUSCULAS
zuul:
  ignored-services: "*"
  #prefix: /api
  routes:
    bitcoindadapter: /bitcoindAdapter/**
    txmempool: /txmempool/**
server:
  port: 8443

